#include "config.h"

// 전역 설정 변수 정의 및 초기값 설정

namespace config {
    
    const std::string FILE_PATH = "dummy_file1.txt";
    const size_t BUFFER_SIZE = 4096;
    const bool ENABLE_LOGGING = true;
    const size_t MAX_LINES = 10000;
}